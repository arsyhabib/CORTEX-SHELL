# CORTEX-SHELL

Clean, reproducible shell template for CORTEX medical education platform.

## Fixes Applied
- Surface ID gap filled (id:5 reserved placeholder)
- Home button repositioned (no FAB overlap)
- Search logic: content before keyword shortcuts
- Sidebar docked mode debounced (smooth resize)
- URL lecture/exam params for shareable links
- Welcome page clears `?page=1` on show
- Auto-generated build tag
- Template versioning (CORTEX_SHELL_VERSION)
- PagePlaceholder component for reserved surfaces

## Usage
```bash
npx serve .
```
